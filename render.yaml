services:
  - type: web
    name: jeopardy-multiplayer
    runtime: python
    buildCommand: |
      pip install -r requirements.txt &&
      gunzip -kf jeopardy.db.gz &&
      chmod +x start.sh
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
